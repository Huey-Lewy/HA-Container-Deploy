# HTTP-only; no automatic HTTPS and no :443 site defined
:80 {
    reverse_proxy web:80 {
        lb_policy random
        health_uri /health
        health_interval 10s
        health_timeout 2s
        health_status 200
    }
    log {
        output stdout
        format console
    }
}